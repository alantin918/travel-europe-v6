# 旅行書產出指令（貼給 AI 用）

## 使用方式
1. 開一個新對話
2. 貼上「**指令 A**」＋ 整份 `旅行書-素材.md`
3. AI 產出封面與 D1–D2 後，之後每次貼「**指令 B**」續產
4. 全部產完後貼「**指令 C**」做附錄與檢查

---

## 指令 A（第一次貼，連同素材一起）

```
你是旅遊手冊的編輯與排版設計師。我要把下面這份已經查證過的行程，
排成一本可以列印的 A5 旅行書（12 天，2026/11/11–11/22，兩人同行）。

【最重要的規則 —— 這是「轉換」不是「創作」】
1. 素材裡的所有時間、價格、地址、店名、班次、營業時間，
   一律原樣保留。不准改寫、不准四捨五入、不准換算單位。
2. 不准補充素材裡沒有的資訊。你不知道的就不要寫。
   如果你覺得某處資訊不足，在該處標註 ❓ 並列在最後的「待確認」清單，
   不要自行填補。
3. 素材裡的 🔴（必做／有風險）、⚠️（注意）、💡（建議）、✅（已確認）
   這些標記要保留，它們是刻意分級的。
4. 不要加入你自己的旅遊建議、景點介紹、歷史典故。
   這本書是給已經規劃完的人現場用的，不是給人做夢的。

【輸出格式】
- 單一 HTML 檔，CSS 內嵌，不要外部連結或 CDN
- 針對 A5 直式紙張設計：@page { size: A5; margin: 12mm }
- 每一天獨立一頁，用 page-break-after: always 分頁
- 中文字體用 system-ui / "Noto Sans TC" / sans-serif 的 fallback 堆疊
- 黑白列印也要清楚：不要用淺色底深色字，重點用粗體與框線而非顏色
- 每頁頁尾放頁碼與日期

【每一天的頁面結構】
① 頁首橫條：D 幾、日期、星期、城市、一句話主題
② 摘要標籤列（素材裡「摘要：」那一行）
③ 時間軸：左欄時間、右欄內容，用表格或 flex 對齊
④ 重點與備註：條列，保留原本的 emoji 分級
⑤ 頁尾留 15mm 空白當「現場手寫區」

【現在要做的】
先產出：封面 ＋ 目錄 ＋ D1 ＋ D2。
產完停下來，等我說「繼續」。不要一次全部產完。
```

---

## 指令 B（每次續產時貼）

```
繼續產出 D3 與 D4，格式與前面完全一致。
一樣不要改寫素材內容，產完停下來等我指示。
```
（把 D3/D4 換成下一組即可，建議一次兩天）

---

## 指令 C（全部產完後貼）

```
現在產出最後的附錄頁，內容取自素材的「📋 附錄：基本資料」：
① 航班四段（表格）
② 住宿六筆（表格，含地址）
③ 已訂已付的關鍵項目（含時間，做成 checklist）
④ 緊急／實用資訊（112、road.is、vedur.is、小費規則、冰島沒有 Uber）
⑤ 空白頁兩頁：一頁「消費記錄」表格、一頁「筆記」

最後另外列出：
- 你在整本書裡標了 ❓ 的所有位置
- 你發現素材前後矛盾的地方（如果有）
不要自己修正矛盾，只要指出來。
```

---

## 備用：如果 AI 有讀網頁的能力

```
請讀取 https://alantin918.github.io/travel-europe-v6/
的「每日行程」分頁，把 12 天排成可列印的 A5 旅行書。
規則與格式同上（貼上指令 A 的【】三個區塊）。
⚠️ 只取那一頁的內容，不要引用你自己對倫敦或冰島的既有知識。
```

---

## 實務提醒

- **12 天一次產完一定會被截斷** —— 分段是必要的，不是保守
- **每段產完先看一眼時間有沒有被動過** —— 這是 AI 最常出錯的地方
- 產出 HTML 後，用瀏覽器「列印 → 另存 PDF」，紙張選 A5、邊界選「無」
- 若要雙面列印，在指令 A 的格式區加一句：
  `每天固定佔兩頁（正反面），不足處留白`
